# cura-connect/config.example.md
# Copy this file to config.md and fill in your own values.
# config.md is gitignored — never commit it.

## recman API
RECMAN_API_KEY=your-recman-api-key

## Gmail OAuth2 credentials
GMAIL_CREDENTIALS_PATH=~/.config/cura-connect/gcp-oauth.keys.json
GMAIL_TOKEN_PATH=~/.config/cura-connect/gmail-token.json
