Read and execute the workflow at `workflows/job-application/WORKFLOW.md`.

If arguments were provided with this command (e.g. a URL), pass them as the job input and run in manual mode. Otherwise run in automatic search mode.
